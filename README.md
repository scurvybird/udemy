# base_project

Add a new thing

## Commit Message Rules

ALWAYS use Conventional Commmit syntax
    feat: message
    fix: message
    chore: message

## Feature Branch workflow

1. `git checkout -b feat/<name>`
1. complete the lesson
1. `git checkout main`
1. merge main to the feature branch `git merge --no-ff feat/<name>`

## Udemy Lesson 5

Variables = information with a value attached to it and stored for later use

Data types
1. String (class:str)= a piece of text
1. Integers (class:int)= whole numbers (no decimals)
1. Floats (class: float)= numbers with a decimal value attached
1. Booleans = true/false values

Concatenating = combining data variables together for a single combined string

Casting = converting a variable from one data type to another

## Commenting on Code

#This is a comment

data in code that will not be executed

reserve code that was written to be accessible at a later time

help yourself and others to read the code that was written (intructions, comments, questions, etc.)

## Booleans

determines if a variable's value is true or false based on conditional statements

conditions include:
1. < (less than)
1. > (greater than)
1. == (equal to)
1. ! (not)

conditions can be combined with following statements:
1. & (and)
1. && (only and)
1. | (or)
1. || (only or)
1. &| (and/or)

values can be assigned to true or false statements with:
1. if
1. elif
1. else

## Flowchart Programming

a visual chart used to illustrate the flow of logic for a given algorithm

1. Circles start the algorithm (start/stop)
1. Squares set variables and assign their values (do something)
1. Diamonds test conditions based on the values (make decision)